# PostgreSQL configuration
#
# Install PostgreSQL:
#   macOS:  brew install postgresql@17
#   Ubuntu: sudo apt-get install postgresql
#
#   Ensure the pg gem is defined in your Gemfile
#   gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  primary:
    <<: *default
    database: hpees_shelf_development
  primary_shard_demo:
    <<: *default
    database: hpees_shelf_demo_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  primary:
    <<: *default
    database: hpees_shelf_test
  primary_shard_demo:
    <<: *default
    database: hpees_shelf_demo_test

# Production uses DATABASE_URL (auto-injected by Railway).
# All Solid gem tables (cache, queue, cable) share the same database
# since their table names are uniquely prefixed.
production:
  primary:
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
  primary_shard_demo:
    <<: *default
    url: <%= ENV["DEMO_DATABASE_URL"].to_s.empty? ? ENV["DATABASE_URL"] : ENV["DEMO_DATABASE_URL"] %>
  cache:
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
    migrations_paths: db/cache_migrate
  queue:
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
    migrations_paths: db/queue_migrate
  cable:
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
    migrations_paths: db/cable_migrate
