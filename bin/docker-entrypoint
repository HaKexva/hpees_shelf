#!/bin/bash -e

# Run pending migrations on every deploy.
# Use db:migrate (not db:prepare) to avoid falling back to schema:load which wipes data.
echo "Running db:migrate..."
./bin/rails db:migrate
echo "db:migrate completed."

exec "${@}"
