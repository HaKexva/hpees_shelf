<% content_for :title, "新增屆數" %>

<div class="mx-auto md:w-2/3 w-full">
  <div class="mx-auto">
    <h1 class="font-bold text-4xl mb-6">新增屆數</h1>

    <%= render "form", in_need: @in_need %>

    <p class="mt-4">
      <%= link_to "返回屆數列表", in_needs_path, class: "rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 font-medium" %>
    </p>
  </div>
</div>
